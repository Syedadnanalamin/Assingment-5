 What is the difference between var, let, and const?
 Answer: var can be access from any of the js document , where let are binded in a scope, let can be updated where const can't be update.
2️⃣ What is the spread operator (...)?
 Answer:spread oparator create a new array from old array without any loop and any major things;


3️⃣ What is the difference between map(), filter(), and forEach()?

Answer:map can copy arrays and returned to a new array, filture return another array with a new element which meet any condition, for each travarse each element and perform any task;
4️⃣ What is an arrow function?

Answer: arrow function works like a normal function but is easy to handle and very handy to use;
5️⃣ What are template literals?

Answer:In template literals we can use variable with a string and update and manipulate data dynamically